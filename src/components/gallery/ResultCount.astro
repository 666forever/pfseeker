---
interface Props {
  count: number;
  label: string;
}

const { count, label } = Astro.props;
---

<p class="result-count" aria-live="polite">
  {count === 1 ? `1 ${label}` : `${count} ${label}s`}
</p>
