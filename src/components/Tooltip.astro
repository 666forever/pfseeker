---
interface Props {
  id: string;
  text: string;
}

const { id, text } = Astro.props;
---

<span class="tooltip">
  <span aria-describedby={id}><slot /></span>
  <span id={id} class="tooltip__content" role="tooltip">{text}</span>
</span>
